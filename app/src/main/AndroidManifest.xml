<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.latihancodelabs">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ClickableImages.OrderActivity"
            android:label="Clickable Images"
            android:parentActivityName=".ClickableImages.ClickableImagesActivity"></activity>
        <activity android:name=".ClickableImages.ClickableImagesActivity" />
        <activity android:name=".Debuger.DebugerActivity" />
        <activity android:name=".ImplicitActivity" />
        <activity
            android:name=".IntentActivity"
            android:label="Activities and Intents"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".SecondActivity"
            android:label="Second Activity"
            android:parentActivityName=".IntentActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>